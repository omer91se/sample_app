<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      the form contains <%= pluralize(@user.errors.count, "error") %>.
    </div>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <% unless msg.include?("digest") %>
          <li>* <%= msg %></li>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>